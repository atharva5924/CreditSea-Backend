
export enum UserRole {
    ADMIN = 'admin',
    VERIFIER = 'verifier',
    USER = 'user'
  }
  
export enum ApplicationStatus {
    PENDING = 'pending',
    APPROVED = 'approved',
    REJECTED = 'rejected'
  }
  